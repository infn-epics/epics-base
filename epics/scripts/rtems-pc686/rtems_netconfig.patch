diff --git a/modules/libcom/RTEMS/rtems_netconfig.c b/modules/libcom/RTEMS/rtems_netconfig.c
index d9267cc..f8a4095 100644
--- a/modules/libcom/RTEMS/rtems_netconfig.c
+++ b/modules/libcom/RTEMS/rtems_netconfig.c
@@ -71,13 +71,11 @@ static struct rtems_bsdnet_ifconfig fxp_driver_config = {
     rtems_fxp_attach,                   /* attach function */
     &ne2k_driver_config,             /* link to next interface */
 };
-extern int rtems_3c509_driver_attach (struct rtems_bsdnet_ifconfig *, int);
-static struct rtems_bsdnet_ifconfig e3c509_driver_config = {
-    "ep0",                              /* name */
-    rtems_3c509_driver_attach,          /* attach function */
-    &fxp_driver_config,                 /* link to next interface */
-};
-#define FIRST_DRIVER_CONFIG &e3c509_driver_config
+/*
+ * RTEMS-pc686 (VME SBCs like the VMIVME-7750): no ISA 3c509 probing, whose
+ * attach function also changed signature in RTEMS 6 (declared in bsp.h)
+ */
+#define FIRST_DRIVER_CONFIG &fxp_driver_config
 #else
 
 # if defined(__PPC)
